apiVersion: v1
kind: LimitRange
metadata:
  name: exam-limits
  namespace: exam
spec:
  limits:
    - default:
        cpu: "250m"
        memory: "128Mi"
      defaultRequest:
        cpu: "100m"
        memory: "64Mi"
      type: Container
