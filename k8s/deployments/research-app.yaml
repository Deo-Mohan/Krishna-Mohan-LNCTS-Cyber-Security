apiVersion: apps/v1
kind: Deployment
metadata:
  name: research-app
  namespace: research
  labels:
    app: research-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: research-app
  template:
    metadata:
      labels:
        app: research-app
    spec:
      serviceAccountName: research-app-sa
      automountServiceAccountToken: false
      containers:
        - name: portal
          image: research-app:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: PORT
              value: "8084"
            - name: DB_HOST
              value: "research-db"
            - name: DB_PORT
              value: "5432"
            - name: DB_USER
              value: "research_user"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: research-db-secret
                  key: db-password
            - name: DB_NAME
              value: "research_db"
            - name: STRICT_DB_CHECK
              value: "true"
          ports:
            - containerPort: 8084
              name: http
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "128Mi"
              cpu: "250m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8084
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8084
            initialDelaySeconds: 10
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
