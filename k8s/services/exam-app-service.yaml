apiVersion: v1
kind: Service
metadata:
  name: exam-app
  namespace: exam
  labels:
    app: exam-app
spec:
  type: ClusterIP
  ports:
    - port: 8083
      targetPort: 8083
      name: http
  selector:
    app: exam-app
