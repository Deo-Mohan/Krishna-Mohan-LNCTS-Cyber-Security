apiVersion: v1
kind: Service
metadata:
  name: exam-db
  namespace: exam
  labels:
    app: exam-db
spec:
  type: ClusterIP
  ports:
    - port: 1521
      targetPort: 1521
      name: postgres
  selector:
    app: exam-db
