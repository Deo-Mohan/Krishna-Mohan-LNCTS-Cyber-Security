apiVersion: v1
kind: Service
metadata:
  name: research-app
  namespace: research
  labels:
    app: research-app
spec:
  type: ClusterIP
  ports:
    - port: 8084
      targetPort: 8084
      name: http
  selector:
    app: research-app
