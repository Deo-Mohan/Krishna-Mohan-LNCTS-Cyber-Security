apiVersion: v1
kind: Service
metadata:
  name: research-db
  namespace: research
  labels:
    app: research-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app: research-db
